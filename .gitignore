.DS_Store
*.log
*.swp
.idea/
.vscode/

# Resident agent build output. The .app bundle is assembled locally by
# scripts/build-agent.sh; nothing signed or prebuilt is committed. The native
# bundle must be installed before the compatibility hook launchers can run.
agent/.build/
build/
hooks/bin/

# Playwright MCP page snapshots — browser scratch output, not project source.
# Swept into a commit once by `git add -A`; kept out of the index since.
.playwright-mcp/

__pycache__/
*.pyc
